export type FormInputs = {
  dishName: string;
};

export type PrepTimeProps = {
  hours: number;
  minutes: number;
  seconds: number;
};

export interface IErrorProps {
  errorCode: number;
  errorMsg: string;
}
